# 佈局元件 (Layout Components)

此目錄包含構成應用程式整體佈局和框架的全局性 React 元件。

## 子目錄

- **`core/`**: 存放應用程式的核心外殼 (App Shell)、頁首 (Header) 和全局 Provider。
- **`navigation/`**: 存放所有與導航相關的元件，如側邊欄 (Sidebar) 和麵包屑 (Breadcrumb)。
- **`shared/`**: 存放可在佈局多個部分共享的小型元件，如 Logo。
